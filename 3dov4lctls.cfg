#[gcode_shell_command set_cam_crop]
#command: v4l2-ctl -d /dev/v4l/by-id/usb-3DO_3DO_NOZZLE_CAMERA_V2_3DO-video-index0 --set-ctrl=pan_absolute=$0 --set-ctrl=tilt_absolute=$1 --set-ctrl=zoom_absolute=$2
#timeout: 2.0
#verbose: False

#[gcode_shell_command reset_cam_crop]
#command: v4l2-ctl -d /dev/v4l/by-id/usb-3DO_3DO_NOZZLE_CAMERA_V2_3DO-video-index0 --set-ctrl=pan_absolute=0 --set-ctrl=tilt_absolute=0 --set-ctrl=zoom_absolute=0
#timeout: 2.0
#verbose: False

#[gcode_macro SET_NOZZLE_CROP]
#description: Crop into 3DO stream using exact hardware step rounding
#gcode:
    # Get raw inputs or set defaults
#    {% set raw_pan = params.PAN|default(0)|int %}
#    {% set raw_tilt = params.TILT|default(0)|int %}
#    {% set zoom = params.ZOOM|default(60)|int %} # Defaulting to max crop window
    
    # Force Pan and Tilt to snap to the nearest 3600 step interval
#    {% set pan = ((raw_pan / 3600)|round(0)|int) * 3600 %}
#    {% set tilt = ((raw_tilt / 3600)|round(0)|int) * 3600 %}

    # Constrain values to your safe limits
#    {% set pan = [[-648000, pan]|max, 648000]|min %}
#    {% set tilt = [[-648000, tilt]|max, 648000]|min %}
#    {% set zoom = [[0, zoom]|max, 60]|min %}
    
#    RUN_SHELL_COMMAND CMD=set_cam_crop PARAMS="{pan} {tilt} {zoom}"

#[gcode_macro RESET_NOZZLE_CROP]
#description: Reset 3DO nozzle camera back to uncropped stream
#gcode:
#    RUN_SHELL_COMMAND CMD=reset_cam_crop


[gcode_shell_command v4l2-ctl]
# Uses a bash command expansion to automatically find any device containing "3DO" in their name since it can differ depending on their firmware/version
command: bash -c 'v4l2-ctl -d $(ls /dev/v4l/by-id/*3DO* | head -n 1) "$@"' --
timeout: 5.0
verbose: True

########################################
# 3DO nozzle camera Available Settings
########################################

[gcode_macro cam_settings]
description: Output all available v4l2 camera controls to the console
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-l"

########################################
# 3DO nozzle camera LED
########################################

[gcode_macro LED_ON]
description: Turn Nozzle Camera light ON
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c backlight_compensation=1"

[gcode_macro LED_OFF]
description: Turn Nozzle Camera light OFF
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c backlight_compensation=0"    

[gcode_macro TOGGLE_CAM_LED]
description: Smart toggle for the 3DO nozzle camera LED
# This initializes the variable so Klipper recognizes it
variable_state: 0
gcode:
    {% if printer["gcode_macro TOGGLE_CAM_LED"].state == 0 %}
        SET_GCODE_VARIABLE MACRO=TOGGLE_CAM_LED VARIABLE=state VALUE=1
        RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c backlight_compensation=1"
        RESPOND TYPE=echo MSG="Camera LED turned ON"
    {% else %}
        SET_GCODE_VARIABLE MACRO=TOGGLE_CAM_LED VARIABLE=state VALUE=0
        RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c backlight_compensation=0"
        RESPOND TYPE=echo MSG="Camera LED turned OFF"
    {% endif %}

[gcode_macro RESET_CAM_LED_STATE]
description: Force reset the macro toggle state to OFF if it gets out of sync
gcode:
    SET_GCODE_VARIABLE MACRO=TOGGLE_CAM_LED VARIABLE=state VALUE=0
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c backlight_compensation=0"
    RESPOND TYPE=echo MSG="Camera LED state hard reset to OFF"

########################################
# Color
########################################

[gcode_macro saturation]
description: min=0, max=100, step=1, default=70
gcode:
    {% set saturation = params.SATURATION|default(70) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c saturation="{saturation}

[gcode_macro hue]
description: min=-2000, max=2000, step=1, default=0
gcode:
    {% set hue = params.HUE|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c hue="{hue}

[gcode_macro sharpness]
description: min=1, max=7, step=1, default=1
gcode:
    {% set sharpness = params.SHARPNESS|default(1) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c sharpness="{sharpness}

########################################
# Exposure
########################################

[gcode_macro brightness]
description: min=0, max=64, step=1, default=15
gcode:
    {% set brightness_level = params.BRIGHTNESS|default(15) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c brightness="{brightness_level}

[gcode_macro contrast]
description: min=0, max=95, step=1, default=4
gcode:
    {% set contrast_level = params.CONTRAST|default(4) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c contrast="{contrast_level}

[gcode_macro gamma]
description: min=1, max=300, step=1, default=115
gcode:
    {% set gamma = params.GAMMA|default(115) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c gamma="{gamma}

[gcode_macro gain]
description: (ISO control) min=0, max=480, step=1, default=0
gcode:
    {% set gain = params.GAIN|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c gain="{gain}

[gcode_macro exposure_absolute]
description: exposure_time_absolute min=3, max=2047, step=1, default=166
gcode:
    {% set exposure_absolute = params.EXPOSURE|default(700) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c exposure_time_absolute="{exposure_absolute}

[gcode_macro auto_exposure]
description: Enable automatic exposure handling
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c auto_exposure=3"

[gcode_macro exposure_manual]
description: Enable manual exposure handling
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c auto_exposure=1"

########################################
# Temperature
########################################

[gcode_macro white_balance_manual]
description: Disable automatic white balance adjustment
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c white_balance_automatic=0"

[gcode_macro white_balance_auto]
description: Enable automatic white balance handling
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c white_balance_automatic=1"

[gcode_macro white_balance_temperature]
description: min=2800, max=6500, step=1, default=4600
gcode:
    {% set white_balance_temperature = params.WHITE|default(4600) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c white_balance_temperature="{white_balance_temperature}

########################################
# Other Settings
########################################

[gcode_macro power_line_frequency_Off]
description: Disable ambient light frequency compensation
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c power_line_frequency=0"

[gcode_macro power_line_frequency_50]
description: Set light flicker removal frequency filter to 50Hz
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c power_line_frequency=1"

[gcode_macro power_line_frequency_60]
description: Set light flicker removal frequency filter to 60Hz
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c power_line_frequency=2"

########################################
# Focus
########################################

[gcode_macro focus_manual]
description: focus_automatic_continuous default=0
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c focus_automatic_continuous=0"

[gcode_macro focus_auto]
description: Enable uninterrupted continuous lens autofocus
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c focus_automatic_continuous=1"

[gcode_macro focus_absolute]
description: min=0, max=1023, step=1, default=0
gcode:
    {% set focus_absolute = params.FOCUS|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c focus_absolute="{focus_absolute}

########################################
# Zoom , Tilt , Pan
########################################

[gcode_macro zoom_in]
description: Set zoom to 60
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c zoom_absolute=60"

[gcode_macro zoom_mid]
description: Set zoom to 30
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c zoom_absolute=30"

[gcode_macro zoom_out]
description: No zoom
gcode:
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c zoom_absolute=0"

[gcode_macro Zoom_absolute]
description: zoom_absolute min=0, max=60, step=1, default=0
gcode:
    {% set zoom_absolute = params.ZOOM|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c zoom_absolute="{zoom_absolute}

[gcode_macro Tilt_absolute]
description: tilt_absolute min=-648000, max=648000, step=3600, default=0
gcode:
    {% set tilt_absolute = params.TILT|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c tilt_absolute="{tilt_absolute}

[gcode_macro Pan_absolute]
description: pan_absolute min=-648000, max=648000, step=3600, default=0
gcode:
    {% set pan_absolute = params.PAN|default(0) %}
    RUN_SHELL_COMMAND CMD=v4l2-ctl PARAMS="-c pan_absolute="{pan_absolute}

